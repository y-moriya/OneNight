@model OneNightWerewolf.Models.Game

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Game</legend>

        @Html.HiddenFor(model => model.GameId)

        <div class="editor-label">
            @Html.LabelFor(model => model.GameName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GameName)
            @Html.ValidationMessageFor(model => model.GameName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PlayerNum)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PlayerNum)
            @Html.ValidationMessageFor(model => model.PlayerNum)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
